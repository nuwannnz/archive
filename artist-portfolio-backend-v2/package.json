{
	"name": "artist-portfolio-backend-v2",
	"version": "1.0.0",
	"description": "",
	"main": "index.js",
	"scripts": {
		"test": "echo \"Error: no test specified\" && exit 1",
		"build": "node ./esbuild.config.js && chmod +x ./bundle.sh && ./bundle.sh"
	},
	"keywords": [],
	"author": "",
	"license": "ISC",
	"dependencies": {
		"esbuild": "^0.20.0",
		"glob": "^10.3.10",
		"mongoose": "^8.1.1"
	},
	"devDependencies": {
		"@types/node": "^20.11.16"
	},
	"Transform": "AWS::Serverless-2016-10-31",
	"Resources": {
		"Api": {
			"Type": "AWS::Serverless::Api",
			"Properties": {
				"Name": {
					"Fn::Sub": [
						"${ResourceName} From Stack ${AWS::StackName}",
						{
							"ResourceName": "Api"
						}
					]
				},
				"StageName": "Prod",
				"DefinitionBody": {
					"openapi": "3.0",
					"info": {},
					"paths": {
						"/": {
							"get": {
								"responses": {}
							}
						}
					}
				},
				"EndpointConfiguration": "REGIONAL",
				"TracingEnabled": true
			}
		}
	}
}